<header>
    <section class="hero" style="min-height:auto; padding-top:0px;">
        <div class="row nav-wrapper">
            <div class="col-lg-12 p-0">
                <!--Navbar Start-->
                <nav class="navbar navbar-expand-lg navbar-custom">
@*                    <ul class="navbar-nav navbar-center" style="margin-left:4em;">
                        @if (Culture.Name == "en")
                        {
                            <li class="nav-item">
                                <a class="nav-link" @onclick="@(() => { Culture = new CultureInfo("fr"); })">FR</a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" @onclick="@(() => { Culture = new CultureInfo("en"); })">EN</a>
                            </li>
                        }
                    </ul>*@
                    <div class="collapse navbar-collapse" id="navbarCollapse">
                        <ul class="navbar-nav ml-auto navbar-center" id="mySidenav">
                            <li class="nav-item">
                                <a href="#auction" class="nav-link" @onclick="@(async () => await OnScrollTo("auction"))">Auctions</a>
                            </li>
                            <li class="nav-item">
                                <a href="#how" class="nav-link" @onclick="@(async () => await OnScrollTo("how"))">How it works ?</a>
                            </li>
                            <li class="nav-item">
                                <a href="#pricing" @onclick="@(async () => await OnScrollTo("pricing"))" class="nav-link">Pricing</a>
                            </li>
                            <li class="nav-item">
                                <AuthorizeView>
                                    <Authorized>
                                        <MudMenu AnchorOrigin="Origin.BottomLeft" TransformOrigin="Origin.TopLeft" ActivationEvent="@MouseEvent.MouseOver" FullWidth="true" Style="margin-right:4em; margin-left:20px; margin-top:10px;">
                                            <ActivatorContent>
                                                <MudAvatar Image="@GetAvatarUri()" />
                                                <MudText Style="padding-left: 15px; padding-top: 8px; color:#FFF !important;">@context.User.Identity.Name</MudText>
                                            </ActivatorContent>
                                            <ChildContent>
                                                <MudMenuItem>Profile</MudMenuItem>
                                                <MudMenuItem Link="/signout">Sign Out</MudMenuItem>
                                            </ChildContent>
                                        </MudMenu>
                                    </Authorized>
                                    <NotAuthorized>
                                        <a href="Account/Login" class="btn-signup btn-primary">Sign in</a>
                                    </NotAuthorized>
                                </AuthorizeView>
                            </li>
                        </ul>
                    </div>
                </nav>
                <!-- Navbar End -->
            </div>
        </div> <!-- End row -->
    </section> <!-- end hero -->
</header>